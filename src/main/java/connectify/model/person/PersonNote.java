package connectify.model.person;

import connectify.model.Note;

/**
 * Represents a PersonNote in the address book.
 */
public class PersonNote extends Note {
    /**
     * Constructor for PersonNote.
     * @param content Content of note
     */
    public PersonNote(String content) {
        super(content);
    }
}
